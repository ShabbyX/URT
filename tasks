TODO
- Add optional parameter to URT_GLUE to pass a parameter to init, body and exit functions.  This is to avoid globals.
- Find a way to be able to define and obtain command line parameters both as kernel module and user application.

Known issues
- with linux
- with RTAI (user space)
- with RTAI (kernel space)
